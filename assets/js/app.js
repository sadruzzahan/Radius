document.querySelectorAll('[data-confirm]').forEach(el=>el.addEventListener('click',e=>{if(!confirm(el.dataset.confirm||'Are you sure?'))e.preventDefault()}));

const lat=document.querySelector('[name=latitude]'),lng=document.querySelector('[name=longitude]'),loc=document.querySelector('[data-use-location]');
if(loc&&navigator.geolocation){loc.addEventListener('click',()=>navigator.geolocation.getCurrentPosition(p=>{if(lat)lat.value=p.coords.latitude.toFixed(7);if(lng)lng.value=p.coords.longitude.toFixed(7);loc.textContent='Location added';},()=>alert('Location permission was not available. Enter it manually.')))}

const mobileToggle=document.querySelector('[data-mobile-nav-toggle]'),mobileNav=document.querySelector('[data-mobile-nav]');
function setMobileNav(open){if(!mobileToggle||!mobileNav)return;mobileToggle.setAttribute('aria-expanded',open?'true':'false');mobileToggle.setAttribute('aria-label',open?'Close navigation':'Open navigation');mobileNav.classList.toggle('open',open);document.body.classList.toggle('mobile-nav-open',open)}
if(mobileToggle&&mobileNav){mobileToggle.addEventListener('click',()=>setMobileNav(mobileToggle.getAttribute('aria-expanded')!=='true'));mobileNav.addEventListener('click',e=>{if(e.target.closest('a'))setMobileNav(false)});document.addEventListener('keydown',e=>{if(e.key==='Escape')setMobileNav(false)});window.addEventListener('resize',()=>{if(window.innerWidth>900)setMobileNav(false)})}

function escapeHtml(v){const d=document.createElement('div');d.textContent=v??'';return d.innerHTML}
const chatLog=document.querySelector('[data-chat-log]');
if(chatLog){let lastId=0;let firstLoad=true;let polling=false;const id=chatLog.dataset.conversation;const pollChat=async()=>{if(polling)return;polling=true;const wasNearBottom=chatLog.scrollHeight-chatLog.scrollTop-chatLog.clientHeight<90;try{const r=await fetch('/api/chat.php?conversation_id='+encodeURIComponent(id)+'&after_id='+lastId,{headers:{Accept:'application/json'}});if(!r.ok)return;const data=await r.json();for(const m of data.items||[]){if(Number(m.id)<=lastId)continue;chatLog.insertAdjacentHTML('beforeend',`<div class="bubble ${m.mine?'me':''}"><strong>${escapeHtml(m.sender_name)}</strong><br>${escapeHtml(m.message)}<div class="muted">${escapeHtml(m.created_at)}</div></div>`);lastId=Math.max(lastId,Number(m.id)||0)}if(Number(data.last_id)>lastId)lastId=Number(data.last_id);if(firstLoad||wasNearBottom)chatLog.scrollTop=chatLog.scrollHeight;firstLoad=false}catch{}finally{polling=false}};pollChat();setInterval(pollChat,3000)}

document.querySelectorAll('[data-radar]').forEach(shell=>{const panel=shell.querySelector('.trust-radar-panel');const nodes=[...shell.querySelectorAll('[data-radar-node]')];const card=shell.querySelector('[data-radar-card]');if(!panel)return;const selectNode=node=>{nodes.forEach(n=>n.classList.toggle('selected',n===node));if(!card||!node)return;card.classList.remove('verified','review','danger');card.classList.add(node.dataset.state||'review');const img=card.querySelector('[data-radar-card-image]'),title=card.querySelector('[data-radar-card-title]'),price=card.querySelector('[data-radar-card-price]'),distance=card.querySelector('[data-radar-card-distance]'),trust=card.querySelector('[data-radar-card-trust]'),link=card.querySelector('[data-radar-card-link]');if(img)img.src=node.dataset.image||'';if(title)title.textContent=node.dataset.title||'';if(price)price.textContent=node.dataset.price||'';if(distance)distance.textContent=(node.dataset.distance||'')+' away';if(trust)trust.textContent=node.dataset.trust||'';if(link)link.href=node.dataset.href||'#'};nodes.forEach(node=>{node.addEventListener('mouseenter',()=>selectNode(node));node.addEventListener('focus',()=>selectNode(node));node.addEventListener('click',()=>selectNode(node));node.addEventListener('dblclick',()=>{if(node.dataset.href)location.href=node.dataset.href})});shell.addEventListener('pointermove',event=>{if(window.matchMedia('(pointer:coarse)').matches)return;const rect=shell.getBoundingClientRect();const px=(event.clientX-rect.left)/rect.width-.5,py=(event.clientY-rect.top)/rect.height-.5;shell.style.setProperty('--tilt-x',`${(-py*5).toFixed(2)}deg`);shell.style.setProperty('--tilt-y',`${(px*6).toFixed(2)}deg`)});shell.addEventListener('pointerleave',()=>{shell.style.setProperty('--tilt-x','0deg');shell.style.setProperty('--tilt-y','0deg')})});
