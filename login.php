<?php
require_once __DIR__.'/includes/functions.php';require_once __DIR__.'/includes/csrf.php';
if($_SERVER['REQUEST_METHOD']==='POST'){verify_csrf();$email=strtolower(trim($_POST['email']??''));$password=$_POST['password']??'';$s=db()->prepare('SELECT * FROM users WHERE email=? LIMIT 1');$s->execute([$email]);$u=$s->fetch();if($u&&$u['is_active']&&password_verify($password,$u['password_hash'])){session_regenerate_id(true);$_SESSION['user_id']=(int)$u['id'];flash('success','Signed in successfully.');redirect($u['role']==='admin'?'/admin/dashboard.php':'/');}flash('error','Invalid email or password.');}
$pageTitle='Login';include __DIR__.'/includes/header.php';?><form class="form-card" method="post" style="max-width:520px"><h1>Welcome back</h1><?=csrf_field()?><div class="stack"><div class="field"><label>Email</label><input type="email" name="email" required></div><div class="field"><label>Password</label><input type="password" name="password" required></div><button class="btn">Login</button></div></form><?php include __DIR__.'/includes/footer.php';?>